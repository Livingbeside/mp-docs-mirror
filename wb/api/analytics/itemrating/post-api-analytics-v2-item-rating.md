---
title: Получить отчёт
api: wb-analytics
method: POST
path: /api/analytics/v2/item-rating
operation_id: postV2ItemRating
tags:
  - itemRating
spec_version: analytics
source: "https://dev.wildberries.ru/docs/openapi/analytics"
deprecated: false
content_sha: 567a867daab4bab6
---

# Получить отчёт

`POST /api/analytics/v2/item-rating`

Описание метода

 Метод [доступен](./api-information#tag/authorization/Pravila-ispolzovaniya-tokenov-dostupa-k-API) по
 Персональному токену, 
 Сервисному токену

Метод формирует набор данных об оценках товаров.

Данные отчёта обновляются 1 раз в час.

Лимит запросов на один аккаунт продавца:

| Период | Лимит | Интервал | Всплеск |
| --- | --- | --- | --- |
| 1 мин | 3 запроса | 20 сек | 3 запроса |

## Запрос

**Тело запроса** (`application/json`):

- `brandNames` — array[string]. Список брендов для фильтрации
- `currentPeriod` — object **обязательный**. Текущий период
  - `end` — string<date> **обязательный**. Дата окончания периода. Не ранее 364 суток от вчерашнего дня
  - `start` — string<date> **обязательный**. Дата начала периода. Не ранее 364 суток от вчерашнего дня и не позднее `end`
- `isNotIncludeNmsWithoutSales` — boolean. Не возвращать товары без продаж: - `true` — да, возвращаются только товары с продажами за период, указанный в объекте `currentPeriod` - `false` — нет, возвращаются все товары, если не указаны другие параметры По умолчанию: `False`.
- `limit` — integer<uint32>. Количество товаров в ответе По умолчанию: `100`.
- `nmIds` — array[integer<int32>]. Список артикулов WB для фильтрации
- `offset` — integer<uint32> **обязательный**. Сколько элементов пропустить. Например, для значения `10` ответ начнётся с 11 элемента
- `onlyShadowedNms` — boolean. Возвращаются ли в ответе только скрытые товары: - `true` — да, возвращаются только скрытые из каталога товары - `false` — нет, возвращаются все товары, если не указаны другие параметры По умолчанию: `False`.
- `orderBy` — object **обязательный**. Параметры сортировки
  - `field` — string (feedbackRating, feedbackCount, fiveStar, fourStar, threeStar, twoStar, oneStar, disqualified) **обязательный**. Поле для сортировки: - `feedbackRating` — Рейтинг товара по отзывам - `feedbackCount` — Все отзывы за период - `fiveStar` — Отзывы 5 звёзд - `fourStar` — Отзывы 4 звезды - `threeStar` — Отзывы 3 звезды - `twoStar` — Отзывы 2 звезды - `oneStar` — Отзывы 1 звезда - `disqualified` — Отзывы, исключённые из рейтинга
  - `mode` — string (asc, desc) **обязательный**. Порядок сортировки: - `asc` — по возрастанию - `desc` — по убыванию
- `pastPeriod` — object. Прошлый период для сравнения. Количество дней — меньше или равно `currentPeriod`
  - `end` — string<date> **обязательный**. Дата окончания периода. Не ранее 364 суток от вчерашнего дня и не позднее даты перед началом `currentPeriod`.
  - `start` — string<date> **обязательный**. Дата начала периода. Не ранее 364 суток от вчерашнего дня и не позднее `end`
- `subjectIds` — array[integer<int32>]. Список ID предметов для фильтрации
- `tagIds` — array[integer<int64>]. Список ID ярлыков для фильтрации

## Ответы

**200** — Успешно

- `data` — object **обязательный**. Данные ответа
  - `feedbackIncrease` — object **обязательный**. Прирост оценок
    - `current` — integer **обязательный**. Прирост оценок за период
    - `dynamics` — integer **обязательный**. Динамика по сравнению с предыдущим периодом, %
    - `fiveStar` — object **обязательный**. Отзывы 5 звёзд
      - `current` — integer **обязательный**. Прирост оценок за период
      - `dynamics` — integer. Динамика по сравнению с предыдущим периодом, %
      - `total` — integer **обязательный**. Всего оценок
    - `fourStar` — object **обязательный**. Отзывы 4 звезды
      - `current` — integer **обязательный**. Прирост оценок за период
      - `dynamics` — integer. Динамика по сравнению с предыдущим периодом, %
      - `total` — integer **обязательный**. Всего оценок
    - `oneStar` — object **обязательный**. Отзывы 1 звезда
      - `current` — integer **обязательный**. Прирост оценок за период
      - `dynamics` — integer. Динамика по сравнению с предыдущим периодом, %
      - `total` — integer **обязательный**. Всего оценок
    - `threeStar` — object **обязательный**. Отзывы 3 звезды
      - `current` — integer **обязательный**. Прирост оценок за период
      - `dynamics` — integer. Динамика по сравнению с предыдущим периодом, %
      - `total` — integer **обязательный**. Всего оценок
    - `total` — integer **обязательный**. Всего оценок
    - `twoStar` — object **обязательный**. Отзывы 2 звезды
      - `current` — integer **обязательный**. Прирост оценок за период
      - `dynamics` — integer. Динамика по сравнению с предыдущим периодом, %
      - `total` — integer **обязательный**. Всего оценок
  - `items` — array[object] **обязательный**. Данные по товарам
    - `brandName` — string **обязательный**. Бренд
    - `disqualified` — integer **обязательный**. Отзывы, исключённые из рейтинга
    - `feedbackCount` — object **обязательный**. Все отзывы за период
      - `current` — integer **обязательный**. Прирост оценок за период
      - `dynamics` — integer. Динамика по сравнению с предыдущим периодом, %
    - `feedbackRating` — object **обязательный**. Рейтинг товара по отзывам
      - `current` — number<float64> **обязательный**. Текущий рейтинг
      - `dynamics` — number<float64>. Динамика по сравнению с предыдущим периодом, %
      - `percentile` — number<float64> **обязательный**. Сколько процентов товаров этого предмета у других продавцов имеют рейтинг ниже, чем у этого товара
    - `fiveStar` — object **обязательный**. Отзывы 5 звёзд
      - `current` — integer **обязательный**. Прирост оценок за период
      - `dynamics` — integer. Динамика по сравнению с предыдущим периодом, %
    - `fourStar` — object **обязательный**. Отзывы 4 звезды
      - `current` — integer **обязательный**. Прирост оценок за период
      - `dynamics` — integer. Динамика по сравнению с предыдущим периодом, %
    - `isShadowed` — boolean **обязательный**. Является ли товар скрытым из каталога: - `true` — товар скрыт из каталога - `false` — товар не скрыт из каталога
    - `nmId` — integer<int64> **обязательный**. Артикул WB
    - `oneStar` — object **обязательный**. Отзывы 1 звезда
      - `current` — integer **обязательный**. Прирост оценок за период
      - `dynamics` — integer. Динамика по сравнению с предыдущим периодом, %
    - `pinnedFeedback` — boolean **обязательный**. Отзыв закреплён
    - `rating` — number<float64> **обязательный**. Рейтинг карточки товара
    - `subjectId` — integer<uint64> **обязательный**. ID предмета
    - `subjectName` — string **обязательный**. Название предмета
    - `tagId` — integer<int64> **обязательный**. ID ярлыка
    - `tagName` — string **обязательный**. Название ярлыка
    - `threeStar` — object **обязательный**. Отзывы 3 звезды
      - `current` — integer **обязательный**. Прирост оценок за период
      - `dynamics` — integer. Динамика по сравнению с предыдущим периодом, %
    - `title` — string **обязательный**. Название товара
    - `twoStar` — object **обязательный**. Отзывы 2 звезды
      - `current` — integer **обязательный**. Прирост оценок за период
      - `dynamics` — integer. Динамика по сравнению с предыдущим периодом, %
    - `vendorCode` — string **обязательный**. Артикул продавца
  - `sellerRating` — object **обязательный**. Рейтинг продавца
    - `current` — number<float64> **обязательный**. Текущий рейтинг
    - `dynamics` — number<float64>. Динамика по сравнению с предыдущим периодом, %

**400** — Неправильный запрос

- `detail` — string **обязательный**. Детали ошибки
- `origin` — string **обязательный**. ID внутреннего сервиса WB
- `requestId` — string **обязательный**. Уникальный ID запроса
- `title` — string **обязательный**. Заголовок ошибки

**401** — Не авторизован

- `code` — string. Внутренний код ошибки
- `detail` — string. Детали ошибки
- `origin` — string. ID внутреннего сервиса WB
- `requestId` — string. Уникальный ID запроса
- `status` — number. HTTP статус-код
- `statusText` — string. Расшифровка HTTP статус-кода
- `timestamp` — string<date-time>. Дата и время запроса
- `title` — string. Заголовок ошибки

**403** — Доступ запрещён

- `detail` — string **обязательный**. Детали ошибки
- `origin` — string **обязательный**. ID внутреннего сервиса WB
- `requestId` — string **обязательный**. Уникальный ID запроса
- `title` — string **обязательный**. Заголовок ошибки

**429** — Слишком много запросов

- `code` — string. Внутренний код ошибки
- `detail` — string. Детали ошибки
- `origin` — string. ID внутреннего сервиса WB
- `requestId` — string. Уникальный ID запроса
- `status` — number. HTTP статус-код
- `statusText` — string. Расшифровка HTTP статус-кода
- `timestamp` — string<date-time>. Дата и время запроса
- `title` — string. Заголовок ошибки
